# Read two integer values. Next, calculate the product between these two values and assign this operation to the PROD variable. Then show the PROD variable with corresponding message.

A = int (input())

B = int (input())

PROD = A*B

print ('PROD = {}'.format(PROD))